# Список наблюдения: тикеры, пулы которых собираются принудительно.
#
# Зачем. Основной набор пулов отбирается по обороту, и токен, который
# в топ не проходит, в расчёт не попадает вовсе — не потому, что связка
# через него убыточна, а потому, что о нём нет данных. Всё, что записано
# здесь, добирается отдельным запросом независимо от оборота.
#
# Как править: один тикер в строке, регистр не важен, после # — комментарий.
# Адрес контракта указывать не нужно, поиск идёт по имени в сети из настроек
# (по умолчанию BNB Chain). После правки — git commit и git push; изменения
# подхватятся следующим прогоном сбора, то есть в течение 15 минут.
#
# Проверить, что токен действительно собирается, можно на странице
# «Токены под наблюдением» в приложении.

AAVE
DAI
LINK
UNI
XVS        # Venus
ALPACA     # Alpaca Finance
TWT        # Trust Wallet Token
FDUSD
